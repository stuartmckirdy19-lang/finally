You are reviewing a planning document that AI coding agents will use to implement a software system.
Read `planning/$ARGUMENTS` carefully, then append a `## Review Notes` section at the end
(do not modify existing content) with these subsections:

**Ambiguities & Missing Decisions** — things the spec is silent on or unclear about that two
agents implementing adjacent components could resolve differently, causing integration failures.
Cite the relevant section. Most critical findings first.

**Internal Contradictions** — places where different sections make conflicting assumptions.

**Simplification Opportunities** — features or design decisions that add implementation cost
without proportionate demo value. Be specific about what to cut and why.
